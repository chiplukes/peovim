"""
config.editorconfig — EditorConfig auto-apply

Reads .editorconfig files via the editorconfig package on buffer_opened.
Applies: indent_style, indent_size, tab_width, end_of_line, charset,
trim_trailing_whitespace, insert_final_newline.
"""
